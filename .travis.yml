dist: trusty
language: php

cache:
  directories:
    - $HOME/.composer/cache/files
php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3

before_script:
  - composer install --dev --no-interaction
script: vendor/bin/phpunit
